def main.main():
    let w0 = newchan main.main.w0_0_0, 0;
    spawn main.main$1(w0);
    send w0;
def main.main$1(ch):
    recv ch;
    spawn main.main$2(ch);
def main.main$2(ch):
    spawn main.main$3(ch);
    send ch;
def main.main$3(ch):
    recv ch;
    spawn main.main$4(ch);
def main.main$4(ch):
    spawn main.main$5(ch);
    send ch;
def main.main$5(ch):
    recv ch;

